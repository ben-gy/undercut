{
  "name": "undercut",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Race strategy with no driving — call the pit windows, read the weather, beat the field on a decision.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "gen:icons": "node scripts/gen-icons.mjs"
  },
  "dependencies": {
    "@ben-gy/game-engine": "github:ben-gy/gh-game-engine#v1.2.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0",
    "jsdom": "^25.0.0"
  }
}
